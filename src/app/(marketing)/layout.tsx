import type { ReactNode } from 'react';

/** Auth and onboarding routes — no app browse chrome. */
export default function MarketingLayout({ children }: { children: ReactNode }) {
  return children;
}
